# Git Useful commands (CLI):

            1. git init
            2. git add f1.md
            3. git rm --cache f1.md   // ye staging area se working area mai jane k liye hai
            4. git commit -m "commit message"
 